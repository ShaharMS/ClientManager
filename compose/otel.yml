# Deprecated — use compose/observability.yml --profile traces

services:
  otel-deprecated-notice:
    image: busybox:1.36
    profiles: [deprecated]
    command: ["sh", "-c", "echo Use compose/observability.yml with --profile traces && exit 1"]
