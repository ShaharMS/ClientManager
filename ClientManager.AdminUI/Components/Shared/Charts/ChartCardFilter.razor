<div class="cm-chart-card__filter @(Clients ? "cm-chart-card__filter--clients" : "cm-chart-card__filter--fixed")">
    @ChildContent
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public bool Clients { get; set; }
}
