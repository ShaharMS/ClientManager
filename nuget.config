<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <config>
    <!-- ponytail: Docker builds hit the default 60s per-request limit on slow NuGet downloads -->
    <add key="http_request_timeout" value="600" />
  </config>
</configuration>
