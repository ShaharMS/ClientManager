# Entry point for `docker compose up`. Switch stacks by editing the include list.
#
# Multipod + observability + traffic-gen (full demo; Admin UI on :5100):
include:
  - path: compose/multipod.yml
  - path: compose/observability.yml
  - path: compose/traffic-gen.yml

# Single API + Admin UI:
# include:
#   - path: compose/default.yml
