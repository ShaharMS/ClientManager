services:
  redis:
    image: redis:7.4-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

  storageapi:
    depends_on:
      - redis

volumes:
  redis-data:
