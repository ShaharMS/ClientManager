# Timeline — code-slimdown

- 2026-06-02 - Bootstrap iteration directory and packet files; began Step 1 (Shared Foundation).
- 2026-06-02 - Step 1 complete: records, logger collapse + call-site fixes, enum consolidation, StorageApiRoutes Escape helper. Verified via solution build (-warnaserror clean on Shared) and browser UI (Dashboard + /services render live data, no errors).
- 2026-06-02 - Step 2 complete: DataAccess slim-down (RateLimitStateDatabase + EntityRepository primary constructors; GlobalRateLimitDatabase inheritance→composition + BuildTargetQuery; ClientConfigurationDatabase MutateAsync helper; UsageSnapshotDatabase BuildQuery helper; ResourceAllocationDatabase ForEachAllocationKey helper). Verified: DataAccess build clean, DataAccess.Tests green, full Service + ResourcePool CRUD via API, global-rate-limits search 28 rows, /rate-limits UI grid renders. Net diff ≈neutral (+142/−137) — composition mandate offsets query-dedup deletions (logged under Finding Dispositions).
- 2026-06-02 - Step 2b complete: storage bindings modernized (shared StoreSerialization.JsonOptions removes 4 duplicated fields; MongoDBDocumentStore primary constructor; DocumentStoreFactory GetOrCreate cache helper + extracted Mongo/Redis/cert builders; six storage option classes → records). Verified: DataAccess/StorageApi/Shared build clean, DataAccess.Tests green, refactored Lucene store full CRUD via API (201/200/204/404), refactored JsonFile store full CRUD with on-disk data/services.json round-trip, UI /services edit persisted + survived page reload. Net ≈+6 raw — structural dedup/extraction win (logged under Finding Dispositions).
- 2026-06-04 - Step 3 complete: relocated Storage API domain services into ClientManager.Api/Services/Storage/ (46 .cs files duplicated + namespace-rewritten to ClientManager.Api.Services.Storage.*, Option A per decision-log), added DataAccess project reference, wrote AddInProcessStorageServices DI extension (+ StorageApiMetrics singleton/meter that the StorageApi host had registered in its own Program.cs), wired it into Program.cs after AddPublicApiServices. Verified: API builds clean, DataAccess.Tests green, API host starts with DI ValidateOnBuild passing and in-process hosted services (AllocationCleanup/ResourceAllocation/InstrumentedDocumentStore) running against JsonFile, UI Dashboard renders live data (no regression). Controllers still use HTTP adapters this step; cut-over is Step 4. Diff +6046 (transport layer removed in Step 4).
- 2026-06-04 - Step 4 complete: cut API controllers/services to in-process storage; deleted Services/Internal transport clients, StorageApiRoutes, resilience handler, fail-open filters, StorageApiOptions/HotPathResilienceOptions, StorageProblemResponse, and the entire ClientManager.StorageApi project; updated slnx, docker-compose, _scripts, copilot-instructions, README, testing-checklist. Commit e4b2670: 128 files, +683 / −5220. Verified: slnx build clean, DataAccess.Tests green, grep clean for IRuntimeStateClient/IStatisticsReadClient/StorageApiRoutes/HotPathFailOpen/AddStorageApiClients; removed orphaned appsettings StorageApi/HotPathResilience sections and StorageApiUnavailableException during bookkeeping.
