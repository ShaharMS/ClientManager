apiVersion: 1

datasources:
  - name: ClientManager Prometheus
    uid: clientmanager-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true

  - name: ClientManager Tempo
    uid: clientmanager-tempo
    type: tempo
    access: proxy
    url: http://tempo:3200
